"use strict"

// 扩展运算符的应用
{
    // （1）合并数组
}

{
    // 与解构赋值结合
    const list = [1, 2, 3, 4, 5];
    let [a, ...rest] = list;
    console.log(a);  // 1
    console.log(rest); // [2, 3, 4, 5]
}


// 箭头函数
