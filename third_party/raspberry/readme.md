default username and password:
	username: pi
	passowrd: raspberry

# shutdow and reboot
	$ sudo poweroff

	$ sudo reboot

# password
	$ sudo passwd pi
	>enter new password

# bind IP
	$

# install 
	$ sudo apt-get update
	$ sudo apt-get install git
